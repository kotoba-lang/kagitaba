# Security policy

Only the latest tagged `0.1.x` release receives security fixes. Report vulnerabilities
privately through GitHub Security Advisories for `kotoba-lang/kagitaba`.

Never attach a real 1Password export. Reproduce issues with synthetic fixtures: `.1pux`
is unencrypted and may contain every secret in a vault.
